# The frontend talks to the backend through Vite's dev proxy (/api -> http://localhost:3000)
# configured in vite.config.ts — no env vars needed for local development.
#
# For production, update the proxy target or set an absolute API base URL
# in src/api/client.ts (the BASE constant) to point at your deployed backend.
